function changeText() {
    // Select the element by its ID
    var element = document.getElementById("myElement");

    // Change its text content
    element.textContent = "Task One is Done i.e Text is Changed!";
}
